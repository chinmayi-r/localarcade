# Milestone status

This is an audited state ledger, not a roadmap. Update a row only after checking the milestone's acceptance and forbidden clauses in `agent-plan.md`. Every implementation checkpoint begins from this file.

## Wave 0 architecture program

| Checkpoint | Status | Evidence | Remaining before implementation |
|---|---|---|---|
| Documentation/design contracts — 2026-07-22 | Approved and committed | Product owner approved the actual brief, screen matrix and design foundation; resolved all ten v1 contract questions; approved execution direction/order; and authorized root-owned M-A. The isolated Wave 0 documentation/contracts snapshot is commit `59e6dd1`. | None. |
| M-A — Shared first-slice contracts | Complete, product-owner-approved and committed | Isolated TypeScript and Rust v1 DTOs validate the same 18 positive fixtures and reject the same 11 negative fixtures; canonical runner-handoff SHA-256 and exact 24-hour expiry agree across runtimes; forward versions fail closed; adapter tests prove lossless representable round trips and explicit blocked mappings; import tests prove the core has no UI/domain/network/process dependency. Root `npm.cmd run check` passes 127 tests/build and runner `npm.cmd run check` passes 30 Rust tests/build without adding IPC commands. Isolated commit: `55afbb2`. | None. M-H now consumes the approved M-A portfolio envelope; M-O remains a later checkpoint. |
| M-A/U25 — Compatibility admission receipt | Complete and product-owner-approved | The additive independently versioned receipt preserves the successful M-E assertion plus exact candidate/artifact SHA-256/runtime/OS/architecture/package facts. U28 adds a separately versioned runner-import-bundle whose outer hash covers the unchanged v1 handoff and exact receipt. TypeScript and Rust accept 20 positive fixtures, reject 12 schema negatives and 12 semantic negatives; forward versions and altered bindings fail closed. | The receipt remains an internal admission record, not a cryptographic attestation. The bundle grants no execution authority. |
| M-B — llmfit upstream adapter | Partial; isolated spike implemented | Product owner approved direct `llmfit-core` `v1.1.6` at `aaa2bc179cec214ccdc44501c853b98fba0b343b` with provider discovery, local history, community and localmaxxing evidence disabled. The isolated Rust library accepts explicit input and uses only the embedded catalog plus `analyze_with_config`; 10 tests and source guards pass. The TypeScript proposed request/envelope boundary and research lanes pass 32 tests. | Upstream unconditionally compiles HTTP/provider/update code, so the crate is not linked into the runner. Rust raw output does not enter the TS envelope, M-C has no verified upstream-family crosswalk, M-F receives nothing, and embedded-data licensing remains unreviewed. Do not mark complete or ship. |
| M-C — Exact artifact registry contract adapter | Complete and committed | `547d015` maps immutable artifact identity and all provenance fields into M-A, preserves snapshot freshness/lifecycle/quarantine semantics, and fails closed on stale or invalid snapshots. The M-C→M-E seam was independently reviewed and exercised in the combined gate. | None for the adapter boundary. Registry expansion remains separately reviewed data work. |
| M-D — Hardware target resolver | Complete and committed; U31 producer/preview transport integrated | `b788396` adds TypeScript and Rust adapters for manual, detected, ambiguous, contradictory, unknown and CPU-only hardware states. The current `runner/src-tauri/src/hardware_confirmation.rs` producer compares imported and separately resolved full snapshots by material facts, treats IDs as informational, blocks missing unified-memory/capacity facts, retains lower RAM or registry-normalized device memory only after exact acknowledgement, and stores receipts behind process-local handles. Twelve focused tests cover duplicate/order matching, normalization, acknowledgement and fail-closed cases. The sealed receipt enters M-J through the process-local M-O preview assembler and typed desktop facade. | Non-Windows strong tool identity and hardware detection remain explicit unavailable/manual behavior until platform adapters are approved. |
| M-E — Exact runtime candidate adapter | Complete and committed | `884dac2`, `1d0ddfb` and integration fix `0f53fb9` build only complete exact candidates from M-C identity, enforce nested KV/sampler and numeric schema constraints, and fail closed on unknown product/build/backend/compatibility. | No serving defaults, fit, ranking, download or process behavior belongs here. |
| M-F — Fit and performance assessment adapter | Boundary and admission mechanism complete; trusted capture is CI-proven; real-data handoff partial | `lib/assessment/` maps the preserved fit cores into the approved M-A envelope. The synthetic M-H handoff remains proven. U27 adds a versioned runner-owned `llama-fit-params` receipt: exact artifact/tool/candidate/compatibility/hardware content is SHA-256-bound; two fixed contexts receive three agreeing observations each; caller-asserted local measurement remains blocked. `runner/src-tauri/src/fit_profile_capture.rs` permits only the sealed Windows/CUDA protocol after M-D/M-E/M-I inputs, and M-P exposes a separate preview, acknowledgement and proposed-unreviewed result. `npm.cmd run proof:mf-runner-capture`, root typecheck/surface/boundary tests and `runner/npm.cmd run check` pass. | No live receipt from a selected real artifact has been reviewed into a profile manifest or paired with a concrete owner-approved capacity policy. The Qwen profile remains preserved legacy evidence and is not reconstructed from candidate data. M-B remains unavailable. No `tight` threshold is approved. |
| M-G — Evidence adapter | Complete and committed | `e3689eb` maps exact personal measurements, scoped community evidence, sourced throughput priors and compatible fit components without upgrading claims. Identity mismatches, contradictory records and lossy GPU/offload pool splits fail closed. | Evidence collection, public contribution and ranking policy remain outside this adapter. |
| M-H — Compatibility-only recommendation portfolio | Boundary complete; synthetic handoff complete; real handoff partial | `lib/portfolio/` consumes an exhaustive frozen universe of exact M-E candidate/receipt, M-F assessment and M-G evidence entries, protected by a process-local composition brand. It emits only M-A `unranked-compatible`, `coverage-gap` or `nothing-fits` portfolios plus an exhaustive internal disposition ledger. Twenty-three behavior tests and one purity/import proof cover hard constraints, one representative per family, at most five families, no padding, exact-evidence and requested-priority selection, reviewed familiarity tie-breaks, deterministic catalog convention, identity and post-admission mutation, contradictory evidence, unattributed gaps and hostile input. | The U27 capture boundary no longer needs to be invented, but there is still no reviewed live M-F profile/capacity policy to admit. M-O consumes only the synthetic portfolio boundary, and production website/desktop/CLI portfolio paths still bypass M-O. The policy makes no ranking-superiority claim and ranked roles remain unreachable. |
| M-I — Read-only inventory adapter | Complete; explicit selected-file promotion integrated | The committed scanner/adapter remains preserved. The additive path hashes only a user-selected size candidate from the retained scan, holds the file under a Windows read-share guard, checks canonical spelling and before/after metadata, and promotes only one exact SHA-256 plus byte-size registry match. Thirteen inventory contract tests and the M-O preview tests pass. | No automatic broad hashing was added. Promotion returns a non-authorizing process-local handle. Strong equivalent file guards remain unavailable outside Windows. |
| M-J — Existing-engine verification adapter | Complete for preparation/preview and fixture/CI-proven controlled execution | Prior M-J evidence remains. U33–U36 add sealed `llama-bench-v1` and fixed `existing-llama-cli-v1` protocols. A genuine `prepare_verification` plan is proven reachable: the benchmark runs first and supplies the structured post-load backend/build/runtime receipt required before fixed mechanical quick checks run. The complete sealed sampler is mapped exactly into `llama-cli`; `llama-bench` validates but does not exercise sampler settings. One canonical Windows local-drive path is established and preserved plan→spawn→report, including verbatim-drive form; alias/symlink/UNC/device routes fail closed. Official output admission retains valid earlier benchmark/check observations on later stop/failure and never substitutes planned facts for observations. The typed desktop facade consumes the approved sequence; full runner and GitHub Windows CI gates pass. | Quick-only execution remains blocked because it cannot produce the approved structured runtime-load/backend receipt. No pinned real `llama-cli` fixture or live external model run was proven. Windows Job Object control is not T7 sandboxing; child network/filesystem isolation remains unenforced. |
| M-O — Application orchestration | Partial; imported and direct-local capture previews plus typed process-local execution transport are consumed by the desktop adapter | Rust below M-O owns the fixed verification and local-capture policy contents. The direct path creates an opaque manual-flow handle after consent-triggered detection, then admits only a registry-exact selected artifact, observed tool build and visible context choice. It constructs the existing exact candidate and experimental compatibility receipt internally; M-P cannot author either. The imported path remains unchanged. Eighty-six Rust library tests plus focused preview/inventory suites pass. | U27 still lacks a live reviewed profile and therefore still prevents a real finder portfolio. No live model proof, restart persistence/recovery, download, network, upload or public Arena was added. |
| M-P — Website, desktop and CLI surfaces | Partial; approved desktop D1–D6 path and direct local U27 capture onboarding implemented, website and CLI incomplete | `runner/src/runner-application.ts` remains the only desktop use-case facade. The primary welcome action is **Check this computer**; website JSON import is an advanced alternative. The UI displays detected hardware, requires explicit scan/selection/tool/side-effect actions, collects a plain-language context target and previews the exact fixed capture configuration. Twenty-two surface tests and eleven boundary tests cover manual and imported journeys and prevent direct lower-module IPC use. Desktop and 390 px mobile browser inspection show no horizontal overflow and preserve the action hierarchy. | The website still calls the legacy recommender, C1–C5 CLI commands do not exist, and no live selected-artifact U27 or external-model run proves the native desktop path. Restart recovery, persistence, downloads, network, uploads, Arena and T7 isolation remain unavailable. |

| Milestone | Status | Evidence | Remaining before completion |
|---|---|---|---|
| M-1 — Evidence foundation | Complete | Evidence types, scope policy and fail-closed contract tests landed in `1426c86`; full gate suite subsequently remains green. | None. |
| M0 — Trusted CI hardening | Complete | Pinned CodeQL, dependency review, Scorecard, Harden Runner, Dependabot, security ledger and zero-data test landed in `6859a07`; no floating `@v` action tags; full gate suite green. | Hosted workflow runs will provide operational confirmation once this source is mirrored to GitHub, but the repository acceptance clauses are satisfied. |
| M1 — Artifact registry | Complete (expanded 2026-07-20) | Trusted-publisher Hub discovery pins 35 popularity-selected repository revisions plus one exact reviewed artifact route. Deterministic admission now produces 452 artifacts across 28 families with complete field provenance and quarantines 426 invalid/unsupported records. The explicit route admits only Qwen3-4B-Instruct-2507 Q4_K_M from trusted publisher `unsloth`; its upstream LFS SHA-256 matches the local measured file and it was manually promoted. Broad discovery retains its pipeline-tag rule. Importer, schema, provenance, scale, quarantine and production-boundary tests pass. | None. |
| M2 — Fit math | Complete (expanded 2026-07-20) | Pure byte-level `fit()` module with explicit weights, K/V profiles, runtime buffers, OS/display reserves and safety margin; 10 sourced llama.cpp allocation-log goldens plus monotonicity, physical-bound, breakdown, mixed-K/V, fail-closed and purity tests. An additive `fitMemoryPools()` primitive now models device and host capacity independently for partial offload, with per-pool failure and monotonicity tests. M4/M5 consume only admitted profiles through a separate candidate-evaluation adapter while all arithmetic remains I/O-free. | Wire two-pool profiles only after exact system RAM and measured per-pool components are present; runtime-specific profiles remain integration data, not inferred by M2. |
| M3 — Throughput priors | Complete (expanded 2026-07-19) | Fourteen Tier-1 LocalScore result-page priors (126 workload samples): five CPU rows plus nine GPU/Apple rows covering RTX 4090, RTX 5070 Ti, RTX 3070 Ti Laptop, GTX 1660, and Apple M1 Pro/M2/M5 Pro across all three size bands. Backend derivation (cuda/metal) and the integrated-vs-gpu pooling boundary are logged in DECISIONS.md. Data extracted from result pages' embedded structured JSON, envelope-only, never point estimates. Unknown-GPU golden updated: inherits widened GPU evidence, still cannot inherit CPU rows. Full gate suite green (95 tests). | Summary-only rows remain inadmissible; more accelerators can be admitted the same way. |
| M4 — Engine on real data | Complete (first exact GPU profile 2026-07-20) | Production consumes the generated artifact registry, pure fit math and product-isolated priors through explicit candidate adapters. Candidates declare compatible platforms and may require exact accelerator IDs. The first GPU candidate is Qwen3-4B-Instruct-2507 Q4_K_M, restricted to RTX 3060 Laptop + llama.cpp b10061 CUDA full offload; checked-in raw fit observations feed independent VRAM/RAM checks and both requirements appear in the UI. Tests cover exact identity, CUDA/AMD isolation, exact-device gating, and failure of either memory pool. No throughput is shown for this candidate. | Expand one exact measured configuration at a time; do not generalize local device observations without an explicit evidence rule. |
| M5 — Evidence UI | Complete | Card metrics and configuration fields flow through compile-enforced `DisplayValue` provenance wrappers; a type-level expect-error test rejects badge-less numeric display data. Collapsed cards show fit, throughput availability, maximum feasible context and artifact size; expanded cards show the complete runtime identity and source links. Catalog age is visible, Tree A UI states are tested, and evidence requests are explicitly browser-local with no network path. Ranking/evaluation/policy/presentation modules plus `ALGORITHM.md` expose every editable decision boundary. Full `npm run check` passes. | None. Broader usefulness still depends on admitting more sourced runtime/hardware profiles; M5 does not fabricate coverage. |
| M6 — Freshness | Complete | Registry schema v4 records `lastIngestSucceededAt`; the production build runs a pure seven-day freshness gate that fails closed for stale, invalid or future timestamps. A twice-weekly SHA-pinned GitHub workflow runs discovery, ingestion and the full gates, then opens or updates a review pull request. New artifact identities default to `triage`, previous lifecycle state and immutable records survive refreshes, and production requires `promoted`. Stale-boundary, thrown stale-build gate, lifecycle golden, generated-registry and workflow-boundary tests pass with the full suite. | None. Scheduled runs require the source to be mirrored to GitHub with Actions and pull-request permissions enabled; promotion remains manual by design. |
| M7 — Localhost middle tier | Retired from product (experiment complete) | The browser-only implementation and its safety gates remain green, but its navigation and production render path were removed by product-owner direction on 2026-07-20. The retained diagnostic code accepts exact loopback endpoints, scores three deterministic tasks, never proxies or persists requests, and fails closed on non-loopback destinations. | Do not invest further. Reuse suite definitions in the runner where they produce verified, configuration-bound evidence; delete the dormant React surface once that migration is complete. |
| M8 — Runner threat model | Complete | `docs/runner-threat-model.md`: T1–T9 threats mapped to cited Tier-1 mechanisms, "runner will never" contract, residual risks, OPEN stop-and-asks. Product owner signed off via chat 2026-07-19; sign-off box checked with attribution. I8 gate open, subject to the OPEN items (updater implementation, Windows GPU sandbox depth, attestation) which remain stop-and-asks for their specific features. | None. |
| M9 — Battles model | Complete (dark) | `lib/battles`: fail-closed pair/vote validation (DNFs rejected per D2X), weighted Bradley–Terry via Hunter's MM iteration with prompter/third-party vote weights from an editable policy module, seeded deterministic bootstrap confidence bands, D8 collecting/live threshold, bucket and kind isolation. 16 hand-computed rating/validation tests plus a production-boundary test proving nothing under `app/` imports the package and `battlesEnabled` is `false`. Full gate suite green. | Turning the flag on is a future product decision requiring a pair-generation and consent surface; ratings remain dark until then. |

## Current checkpoint

| R1 — Tauri skeleton + signed releases | Complete | `runner/` Tauri 2 workspace (Rust 1.97.1 stable-MSVC): identity-only app, core-IPC-only capability, strict CSP, no opener/updater/HTTP crates. Runner gate suite (`npm run check` in `runner/`: typecheck, fmt, clippy -D warnings, cargo test incl. the R1 zero-capabilities test, vite build) green. `runner-ci.yml` + `runner-release.yml` (cosign keyless signing + SLSA provenance; SHA-pinned except the SLSA reusable workflow's documented tag exception) added with ledger rows. winget/Homebrew manifest drafts in `runner/packaging/` with TODO-release fields. Root `tests/runner-boundary.test.mjs` (4 tests) enforces no HTTP/socket/updater crates, core-only capabilities, strict CSP, no request APIs in the frontend. Root gate suite green (105 tests). | Release workflows execute only once the repo is mirrored to GitHub and a `runner-v*` tag is pushed; manifests await the first public release. |

| R2 — Hardware detection (read-only) | Complete | `runner/src-tauri/src/hardware.rs`: consent-gated (button-only) detection — OS/CPU/RAM via sysinfo, GPUs via DXGI with software adapters excluded; compile-time-embedded vendor registry; reconciliation flags memory mismatches and fails closed on unknown devices (B1X); calibration-fingerprint schema defined, unpopulated. 8 Rust tests including fixture reconciliation (known/mismatch/unknown/multi-variant) and a live smoke test — verified on real hardware: RTX 3060 Laptop → Known with variant match, Iris Xe → Unknown/manual. Registry gained vendor-sourced RTX 3060/3070 Ti Laptop entries. Runner + root gates green; boundary tests unchanged (still core-IPC-only, zero network). | Non-Windows GPU enumeration intentionally returns unavailable → manual entry until implemented on those platforms. |

| R3 — Model-store scan (read-only) | Complete | `runner/src-tauri/src/model_store.rs`: consent-gated (button-only) scan of Ollama (read as files, never invoked), LM Studio (both locations), and user-named GGUF folders. Registry matching: digest equality → `verified`, size-only → `candidateBySize` (never silently upgraded), hashing deferred to a later explicit step. Duplicates grouped by digest and by size; unreadables listed per-file; walks capped with reported truncation. 7 integration tests (fixture stores, verified/candidate paths, malformed manifests, dangling blobs, duplicates, missing dirs) plus a live smoke — verified on the dev machine (empty Ollama correctly reported as a normal state after a live-found misclassification fix). Boundary test proves the module contains no write APIs. Runner + root gates green. | Per-file hashing UI (upgrading candidates to verified) belongs to a later slice. |

| R4 (existing-engine slice) — Benchmark | Complete (partial R4, reliability/preflight/tasks expanded 2026-07-20) | Existing `llama-bench` reports preserve complete configuration and raw samples; repeatability gates separate exact stable observations from conditioned observations. A second independent `calibrationEligibility` gate now rejects unstable samples, adverse preflight, unknown power/thermal state, and unavailable or active concurrent-GPU telemetry. Thus a genuine repeatable observation can remain visible without rescaling other recommendations. Read-only preflight samples CPU/RAM/Windows AC; quick tasks run three mechanical fixtures through one checked `llama-cli` spawn site. Real Qwen3-4B validation completed with two passes and one exact-format miss; a Windows stdout-pipe deadlock was found and fixed. | Current builds intentionally produce no clean calibration baseline because trustworthy thermal and concurrent-GPU telemetry are unavailable. Full R4 (bundled engine + T7 sandbox + calibration transfer) remains gated. Artifact hash binding, reviewed telemetry adapters and serving-config import are future slices. |

**Current checkpoint:** the approved Wave 1 M-O producer/adapter and M-P desktop
subset is integrated and committed on `main` through `4dbf7f5`; CI/security
repair commits `572316f`, `472aa7f` and `26aa4d2` preserve the same scope.
U28–U30 retain their prior behavior. U31 binds imported and detected hardware by complete
material facts behind a sealed process-local receipt. U32 binds an explicitly
selected Windows llama.cpp executable to guarded path/hash/reported-build
evidence without claiming backend, authorization or child isolation. The Rust
assembler validates U29 expiry using its own clock, preserves M-I status,
admits only exact verified selections, samples preflight locally and produces
an M-J verification-plan preview with
`grantsExecutionAuthorization: false`.

Seven preview IPC commands exist as a backend boundary; the typed desktop facade
consumes the approved preview/execution routes, while website and CLI do not.
The checkpoint includes no
run/progress/stop/result command, download, upload, HTTP client, updater,
public Arena or actual-backend claim. Predictable process-local handles are
references rather than credentials. Root `npm.cmd run check` passes typecheck,
lint, 136 TypeScript/UI tests, 34 boundary tests, registry freshness and the
production build. Runner `npm.cmd run check` passes typecheck, formatting,
Clippy with warnings denied, 104 Rust tests and its production build. Contracts
validate 20 positive, 12 schema-negative and 12 semantic-negative cases; the
complete M-O checkpoint proof, capability board and all 80 Markdown files pass.
The full root and runner gates, Root CI, Runner CI, CodeQL and Scorecard pass on
`26aa4d2`.

The subsequently approved U33–U37 execution checkpoint adds combined
benchmark-then-fixed-check execution and an opaque typed process-local
transport behind the prepared handle. Fake-runner proofs cover genuine M-J
preparation reachability, exact sampler transport, completed evidence,
stop-during-check retention, replay/cross-identity rejection,
reported-backend mismatch, malformed benchmark/quick output and one-active-run
enforcement (15/15). Transport proofs cover acknowledgements, hostile input,
stale references and terminal stop idempotence (5/5); the combined Rust library
suite passes 80/80. These are
fixture/process-boundary proofs, not a live model run. Quick-only execution
remains blocked without structured benchmark load evidence. The four typed
execution commands now have a typed M-P desktop facade and D1–D6 consumer.
Legacy raw execution registrations are retired while their implementation code
and surface remain preserved as explicitly labelled legacy files.
The current assembled checkpoint gate passes 155 TypeScript/UI tests, 36 root
boundary tests and 164 Rust tests across all targets, plus both production
builds. No pinned external model or `llama-cli` run is evidenced here.

The subsequent manual-onboarding checkpoint makes the approved D1 manual route
primary. It removes the bundle and user-authored capture ID from the ordinary
flow, displays the runner-detected machine before confirmation, and exposes the
exact capture configuration before the separate process consent. The unchanged
website-import route remains under **Advanced**. Focused evidence is 22/22
surface tests, 11/11 boundary tests and 86/86 Rust library tests; desktop and
390 px browser inspection pass. This is a user-operable setup/capture-preview
path, not a live-model, production-recommendation or restart-recovery proof.

The packaging/live-protocol checkpoint found and corrected a real
fixture-to-tool mismatch: `llama-fit-params` build 10061 uses `--mmap`, not the
`llama-bench` `-mmp 1` spelling. Manual capture is now pinned to the exact
proven build and later builds fail closed. The corrected protocol completed
three agreeing observations at 8K and three at 32K against the promoted
Qwen3-4B Q4_K_M artifact on the detected RTX 3060 Laptop GPU. The optimized
runner binary passed a launch smoke test and Tauri produced unsigned x64 MSI
and NSIS test installers. This proves a real capture protocol and package
launch, not UI automation, reviewed M-F admission, a website portfolio,
restart recovery, signing or public-release readiness.
