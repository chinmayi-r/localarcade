# Milestone status

This is an audited state ledger, not a roadmap. Update a row only after checking the milestone's acceptance and forbidden clauses in `agent-plan.md`. Every implementation checkpoint begins from this file.

## Wave 0 architecture program

| Checkpoint | Status | Evidence | Remaining before implementation |
|---|---|---|---|
| Documentation/design contracts — 2026-07-22 | Approved and committed | Product owner approved the actual brief, screen matrix and design foundation; resolved all ten v1 contract questions; approved execution direction/order; and authorized root-owned M-A. The isolated Wave 0 documentation/contracts snapshot is commit `59e6dd1`. | None. |
| M-A — Shared first-slice contracts | Complete, product-owner-approved and committed | Isolated TypeScript and Rust v1 DTOs validate the same 18 positive fixtures and reject the same 11 negative fixtures; canonical runner-handoff SHA-256 and exact 24-hour expiry agree across runtimes; forward versions fail closed; adapter tests prove lossless representable round trips and explicit blocked mappings; import tests prove the core has no UI/domain/network/process dependency. Root `npm.cmd run check` passes 127 tests/build and runner `npm.cmd run check` passes 30 Rust tests/build without adding IPC commands. Isolated commit: `55afbb2`. | None. The approved non-production ranking/audition research checkpoint is active; M-H/M-O remain blocked on its owner review. |
| M-C — Exact artifact registry contract adapter | Complete and committed | `547d015` maps immutable artifact identity and all provenance fields into M-A, preserves snapshot freshness/lifecycle/quarantine semantics, and fails closed on stale or invalid snapshots. The M-C→M-E seam was independently reviewed and exercised in the combined gate. | None for the adapter boundary. Registry expansion remains separately reviewed data work. |
| M-D — Hardware target resolver | Complete and committed | `b788396` adds TypeScript and Rust adapters for manual, detected, ambiguous, contradictory, unknown and CPU-only hardware states. It preserves field origins, never invents capacity, and keeps Apple unified memory as one pool. | Non-Windows detection remains explicit manual/unavailable behavior until a platform adapter is approved. |
| M-E — Exact runtime candidate adapter | Complete and committed | `884dac2`, `1d0ddfb` and integration fix `0f53fb9` build only complete exact candidates from M-C identity, enforce nested KV/sampler and numeric schema constraints, and fail closed on unknown product/build/backend/compatibility. | No serving defaults, fit, ranking, download or process behavior belongs here. |
| M-G — Evidence adapter | Complete and committed | `e3689eb` maps exact personal measurements, scoped community evidence, sourced throughput priors and compatible fit components without upgrading claims. Identity mismatches, contradictory records and lossy GPU/offload pool splits fail closed. | Evidence collection, public contribution and ranking policy remain outside this adapter. |

| Milestone | Status | Evidence | Remaining before completion |
|---|---|---|---|
| M-1 — Evidence foundation | Complete | Evidence types, scope policy and fail-closed contract tests landed in `1426c86`; full gate suite subsequently remains green. | None. |
| M0 — Trusted CI hardening | Complete | Pinned CodeQL, dependency review, Scorecard, Harden Runner, Dependabot, security ledger and zero-data test landed in `6859a07`; no floating `@v` action tags; full gate suite green. | Hosted workflow runs will provide operational confirmation once this source is mirrored to GitHub, but the repository acceptance clauses are satisfied. |
| M1 — Artifact registry | Complete | Trusted-publisher Hub discovery pins 35 repository revisions; deterministic bulk admission produces 451 artifacts across 27 families with complete field provenance and quarantines 426 invalid/unsupported records. Two consecutive ingests produced SHA-256 `1F9BFF8DAC8B8E8D6A009EC7A0205E65C134F34A7CF073B95608BC51E474FC04`; importer, schema, provenance, scale, quarantine and production-boundary tests pass. M4 subsequently removed the demo catalog and connected admitted records through an explicit candidate adapter. | None. |
| M2 — Fit math | Complete | Pure byte-level `fit()` module with explicit weights, K/V profiles, runtime buffers, OS/display reserves and safety margin; 10 sourced llama.cpp allocation-log goldens plus monotonicity, physical-bound, breakdown, mixed-K/V, fail-closed and purity tests. M4/M5 now consume it through a separate candidate-evaluation adapter while the arithmetic remains I/O-free. | None. Runtime-specific profiles for registry artifacts remain integration data, not inferred by M2. |
| M3 — Throughput priors | Complete (expanded 2026-07-19) | Fourteen Tier-1 LocalScore result-page priors (126 workload samples): five CPU rows plus nine GPU/Apple rows covering RTX 4090, RTX 5070 Ti, RTX 3070 Ti Laptop, GTX 1660, and Apple M1 Pro/M2/M5 Pro across all three size bands. Backend derivation (cuda/metal) and the integrated-vs-gpu pooling boundary are logged in DECISIONS.md. Data extracted from result pages' embedded structured JSON, envelope-only, never point estimates. Unknown-GPU golden updated: inherits widened GPU evidence, still cannot inherit CPU rows. Full gate suite green (95 tests). | Summary-only rows remain inadmissible; more accelerators can be admitted the same way. |
| M4 — Engine on real data | Complete | Production consumes the generated artifact registry, pure fit math and product-isolated priors through explicit candidate adapters. The demo catalog and prototype eligibility/speed math are deleted. Typed ranked, unranked, contradictory and nothing-fits outcomes, family nesting and role-labelled top-five behavior are covered by goldens; production UI renders exact artifact/runtime identity and withholds unsupported speed/order claims. `npm run check` passes and production grep tests enforce the boundary. | None. Coverage is deliberately sparse until more runtime-scoped fit, compatibility, task and GPU evidence is admitted; sparse coverage is not filled with inferred configurations. |
| M5 — Evidence UI | Complete | Card metrics and configuration fields flow through compile-enforced `DisplayValue` provenance wrappers; a type-level expect-error test rejects badge-less numeric display data. Collapsed cards show fit, throughput availability, maximum feasible context and artifact size; expanded cards show the complete runtime identity and source links. Catalog age is visible, Tree A UI states are tested, and evidence requests are explicitly browser-local with no network path. Ranking/evaluation/policy/presentation modules plus `ALGORITHM.md` expose every editable decision boundary. Full `npm run check` passes. | None. Broader usefulness still depends on admitting more sourced runtime/hardware profiles; M5 does not fabricate coverage. |
| M6 — Freshness | Complete | Registry schema v4 records `lastIngestSucceededAt`; the production build runs a pure seven-day freshness gate that fails closed for stale, invalid or future timestamps. A twice-weekly SHA-pinned GitHub workflow runs discovery, ingestion and the full gates, then opens or updates a review pull request. New artifact identities default to `triage`, previous lifecycle state and immutable records survive refreshes, and production requires `promoted`. Stale-boundary, thrown stale-build gate, lifecycle golden, generated-registry and workflow-boundary tests pass with the full suite. | None. Scheduled runs require the source to be mirrored to GitHub with Actions and pull-request permissions enabled; promotion remains manual by design. |
| M7 — Localhost middle tier | Complete | Browser-only quick test accepts exact loopback OpenAI-compatible endpoints after explicit action, runs three deterministic mechanically scored tasks sequentially, and reports measured request latency/task outcomes while retaining product, engine and build identity. Hardware remains self-reported; generation throughput is shown only when the endpoint reports it. Mocked endpoint, scorer, unreachable-state, response-contract, in-memory, no-proxy and non-loopback network-boundary tests pass with the full gate suite. | None. Product-specific CORS setup remains dependent on each local server; MLX LM's official guide documents no CORS control, so that route fails closed when the browser blocks it. |
| M8 — Runner threat model | Complete | `docs/runner-threat-model.md`: T1–T9 threats mapped to cited Tier-1 mechanisms, "runner will never" contract, residual risks, OPEN stop-and-asks. Product owner signed off via chat 2026-07-19; sign-off box checked with attribution. I8 gate open, subject to the OPEN items (updater implementation, Windows GPU sandbox depth, attestation) which remain stop-and-asks for their specific features. | None. |
| M9 — Battles model | Complete (dark) | `lib/battles`: fail-closed pair/vote validation (DNFs rejected per D2X), weighted Bradley–Terry via Hunter's MM iteration with prompter/third-party vote weights from an editable policy module, seeded deterministic bootstrap confidence bands, D8 collecting/live threshold, bucket and kind isolation. 16 hand-computed rating/validation tests plus a production-boundary test proving nothing under `app/` imports the package and `battlesEnabled` is `false`. Full gate suite green. | Turning the flag on is a future product decision requiring a pair-generation and consent surface; ratings remain dark until then. |

## Current checkpoint

| R1 — Tauri skeleton + signed releases | Complete | `runner/` Tauri 2 workspace (Rust 1.97.1 stable-MSVC): identity-only app, core-IPC-only capability, strict CSP, no opener/updater/HTTP crates. Runner gate suite (`npm run check` in `runner/`: typecheck, fmt, clippy -D warnings, cargo test incl. the R1 zero-capabilities test, vite build) green. `runner-ci.yml` + `runner-release.yml` (cosign keyless signing + SLSA provenance; SHA-pinned except the SLSA reusable workflow's documented tag exception) added with ledger rows. winget/Homebrew manifest drafts in `runner/packaging/` with TODO-release fields. Root `tests/runner-boundary.test.mjs` (4 tests) enforces no HTTP/socket/updater crates, core-only capabilities, strict CSP, no request APIs in the frontend. Root gate suite green (105 tests). | Release workflows execute only once the repo is mirrored to GitHub and a `runner-v*` tag is pushed; manifests await the first public release. |

| R2 — Hardware detection (read-only) | Complete | `runner/src-tauri/src/hardware.rs`: consent-gated (button-only) detection — OS/CPU/RAM via sysinfo, GPUs via DXGI with software adapters excluded; compile-time-embedded vendor registry; reconciliation flags memory mismatches and fails closed on unknown devices (B1X); calibration-fingerprint schema defined, unpopulated. 8 Rust tests including fixture reconciliation (known/mismatch/unknown/multi-variant) and a live smoke test — verified on real hardware: RTX 3060 Laptop → Known with variant match, Iris Xe → Unknown/manual. Registry gained vendor-sourced RTX 3060/3070 Ti Laptop entries. Runner + root gates green; boundary tests unchanged (still core-IPC-only, zero network). | Non-Windows GPU enumeration intentionally returns unavailable → manual entry until implemented on those platforms. |

| R3 — Model-store scan (read-only) | Complete | `runner/src-tauri/src/model_store.rs`: consent-gated (button-only) scan of Ollama (read as files, never invoked), LM Studio (both locations), and user-named GGUF folders. Registry matching: digest equality → `verified`, size-only → `candidateBySize` (never silently upgraded), hashing deferred to a later explicit step. Duplicates grouped by digest and by size; unreadables listed per-file; walks capped with reported truncation. 7 integration tests (fixture stores, verified/candidate paths, malformed manifests, dangling blobs, duplicates, missing dirs) plus a live smoke — verified on the dev machine (empty Ollama correctly reported as a normal state after a live-found misclassification fix). Boundary test proves the module contains no write APIs. Runner + root gates green. | Per-file hashing UI (upgrading candidates to verified) belongs to a later slice. |

| R4 (existing-engine slice) — Benchmark | Complete (partial R4) | `runner/src-tauri/src/benchmark.rs`: explicit-consent benchmark spawning exactly the user's own `llama-bench` (single boundary-tested spawn site), watchdog kill-timeout, fail-closed JSON parsing pinned to a real-output fixture, results badged `verified-local` with full engine/config identity. Live end-to-end on the dev machine: Qwen3-4B on RTX 3060 Laptop via CUDA, 228 t/s prompt / 4.3 t/s generation at llama-bench defaults. Persona findings logged in `docs/persona-findings.md`; coverage-gap outcome added to the website engine from pass 1. | Full R4 (bundled pinned engine + T7 sandbox + quick-task suite + calibration transfer) remains gated on the Windows GPU sandbox stop-and-ask. User-flag/llama-swap-config benchmarking is backlog item 1. |

**Current checkpoint:** M-C, M-D, M-E and M-G are integrated on
`codex/wave1-domain-adapters`. The combined root gate is green (129 TypeScript
tests, 30 boundary tests and production build); the runner gate is green (40
Rust tests across all targets and production build). M-I is paused on the
inventory-result contract boundary. M-J is paused on Windows-first,
user-supplied-engine and no-network scope decisions. M-B and M-H remain paused
on their recorded owner/research gates. M-O and all surface implementation
remain prohibited until their dependencies and checkpoint decisions close.
